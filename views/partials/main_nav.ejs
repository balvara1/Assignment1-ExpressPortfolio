 <!-- Main Navigation -->
 <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
      <a href="/"><img src="https://i.imgur.com/VQAJuIg.png" alt="Logo" class="img-fluid" width="80" height="80"</a>
      
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ml-auto">

          <!--Active Navigation-->
          <li class="nav-item">
            <a class=
            <% if (title == "Home") { %>
              "nav-link active"
            <% } else { %>
              "nav-link"
              <% } %>
              aria-current="page" href="/home">
              <i class="fas fa-lg fa-home"></i> Home</a>
          </li>

          <li class="nav-item">
            <a class=
            <% if (title == "About") { %>
              "nav-link active"
            <% } else { %>
              "nav-link"
              <% } %>
              aria-current="page" href="/about">
              <i class="fas fa-lg fa-info"></i> About Me</a>
          </li>

          <li class="nav-item">
            <a class=
            <% if (title == "Projects") { %>
              "nav-link active"
            <% } else { %>
              "nav-link"
              <% } %>
              aria-current="page" href="/projects">
              <i class="fas fa-lg fa-laptop-code"></i> Projects</a>
          </li>

          <li class="nav-item">
            <a class=
            <% if (title == "Services") { %>
              "nav-link active"
            <% } else { %>
              "nav-link"
              <% } %>
              aria-current="page" href="/services">
              <i class="fas fa-lg fa-tools"></i> Services</a>
          </li>

          <li class="nav-item">
            <a class=
            <% if (title == "Contact") { %>
              "nav-link active"
            <% } else { %>
              "nav-link"
              <% } %>
              aria-current="page" href="/contact">
              <i class="fas fa-lg fa-phone-alt"></i> Contact</a>
          </li>
      </div>
    </div>
  </nav>